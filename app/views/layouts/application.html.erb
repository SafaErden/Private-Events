<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-primary bg-primary text-white">
  <a class="navbar-brand  text-white" href="#">Private Events</a>
  <button class="navbar-toggler  btn btn-light" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon text-white"></span>
  </button>
  <div class="collapse navbar-collapse  text-white" id="navbarNavAltMarkup">
    <div class="navbar-nav  text-white">
      <%= link_to 'Events', events_path, class: "nav-item nav-link  text-white" %>
      <%= link_to 'Invite People', invites_path, class: "nav-item nav-link  text-white" %>
      <% if session[:id].nil? %>
        <%= link_to 'Log in', sessions_path, class: "nav-item nav-link  text-white" %>
      <% else %>
      <%#= session[:id] %>
        <%= link_to 'Log out', session_path(session), data: {:confirm => 'Are you sure?'}, :method => :delete, class: "nav-item nav-link" %>
      <% end %>
    </div>
  </div>
</nav>
<% if flash[:notice] %>
<div class="border border-success text-success p-3 text-center mb-3">
  <p class="h3 p-0 m-0"><%= notice %></p>
</div>
<% end %>

<% if flash[:alert] %>
<div class="border border-danger text-danger p-3 text-center  mb-3">
  <p class="h3 p-0 m-0"><%= alert %></p>
  </div>
<% end %>
    <%= yield %>
  </body>
</html>
